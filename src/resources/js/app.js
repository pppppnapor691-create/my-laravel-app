// Импортируем JS Bootstrap (обязателен для модалок, dropdown, collapse)
import 'bootstrap/dist/js/bootstrap.bundle.min.js';
// Импортируем SCSS — можно либо тут, либо отдельно через Vite (см. ниже)
import '../sass/app.scss';
